# BNS 자산 대조 및 조정 시스템 업데이트 노트 (RELEASE_NOTES)

이 파일은 BNS 자산 대조 및 조정 도구의 버전별 주요 기능 개선 및 비즈니스 룰 변경 사항을 투명하게 관리하기 위한 공식 릴리즈 노트입니다.

---

## [v1.2.1] - 2026-07-17

개발 지침 및 비즈니스 룰 영구 저장소 고도화 내역입니다.

* **개발 수칙(AGENTS.md) 세부 룰 추가**:
  * **자동 대조 대기(Standby)**: 탭 이동, 필터 변경, 초기 로드 시 `[매칭 시작]` 버튼 클릭 전에는 매칭 연산을 수행하지 않고 대기하도록 명시.
  * **제조번호 검색어 기준**: 초기값 `"시리얼"`, 포함 검색(contains) 지원, 청구 데이터 단독 적용 범위 명시.
  * **여백 초밀착 가이드라인**: 마진/패딩 최소화, 버튼 26px 내외, 하단 리스트박스 고정 스크롤 규칙 명시.

---

## [v1.2.0] - 2026-07-17 (최신 배포 버전)

오늘 적용된 핵심 기능 보완 및 화면 레이아웃 최적화 내역입니다.

### 1. 매칭 알고리즘 필수 일치(AND 조건) 전환
* **엄격한 AND 조건 매칭**: 좌측 사이드바에서 선택(체크)한 대조 기준(사번, 이름, 부서, 사업장)을 **동시에 모두 충족하는 실사 자산만** 1&2영역 매칭 후보로 결합됩니다.
* **미체크 항목 상세 정보 표시**: 체크 해제(False)한 기준들의 일치 여부는 매칭 조건 판정에는 영향을 주지 않되, 1&2영역 테이블의 "매칭 신뢰도" 컬럼에 상세 상태 문구로 누적 출력하여 사용자가 직관적으로 판단할 수 있도록 지원합니다.
  * *출력 예시*: `이름 일치 (사번/부서/사업장 상이)`, `사번/이름 일치 (부서 상이)`
* **최적 매칭 선별**: 필수 조건을 통과한 다수 후보 중 전체 항목 가중치 합산 점수(사번 100점, 이름 50점, 부서 20점, 사업장 10점)가 가장 높은 1순위 자산과 최종 매칭됩니다.

### 2. 엑셀 원본 서식 100% 보존 및 보정이력 로깅 (`exceljs` 이식)
* **서식 완벽 유지**: 저장 엔진을 `exceljs` 라이브러리로 대체하여, 데이터 반영 후에도 기존 엑셀 파일의 **배경색, 폰트 스타일, 셀 테두리선, 줄 높이 및 수식(Formula) 정보**가 전혀 깨지거나 날아가지 않고 완벽하게 보존됩니다.
* **보정이력 컬럼 신설**: 저장 시 청구 데이터 시트의 가장 우측 끝 열에 `"보정이력"` 컬럼을 자동 신설(맑은 고딕 bold, 11pt)합니다.
* **누적 로그 기록 (Audit Trail)**: 수정이 발생할 때마다 해당 자산의 행에 보정 시각과 작업 이력을 기록하며, 재수정 시 기존 로그 밑에 개행(`\n`)하여 보정이력을 계속 누적 보관합니다.
  * *로그 예시*: `[2026-07-17 17:05:12] 보정: 실사 제조번호 [ZZKK...] 반영`

### 3. 모델 필터 리스트박스 다이나믹 연동 및 UI 개선
* **검색어 기반 모델명 실시간 구성**: `"청구 제조번호 검색"` 텍스트박스 키워드(초기값: `"시리얼"`)를 입력하면, 해당 키워드가 들어간 제조번호를 가진 자산들의 고유 모델명 목록들만 다이나믹하게 추출해 리스트박스 아이템으로 재구성합니다.
* **필터 헤더 UI 개편**:
  * 리스트박스 레이블에서 불필요한 `(다중)` 문구를 제거했습니다.
  * 모델 필터 헤더 우측에 **`전체` 선택 체크박스**를 추가해 클릭 한 번으로 목록 전체를 손쉽게 체크/해제할 수 있습니다.
  * **`모델명` 키워드 텍스트박스**를 추가하여 체크박스에 선택되어 있지 않더라도 입력 키워드가 모델명에 포함된 자산을 **OR 조건**으로 매칭 연산에 함께 포함되도록 결합했습니다.

### 4. 프로그램 시작 시 파일 복원 및 자동 조회 기능
* 프로그램 시작(또는 새로고침) 시, 로컬에 저장되어 있던 3개의 파일명과 컬럼 매핑 드롭다운 정보가 자동으로 완벽하게 채워집니다.
* 첫 번째 탭("자산 대조 및 조정")의 대조 테이블이 자동으로 즉시 구동 및 렌더링되어 사용자의 불필요한 클릭 대기 시간을 최소화합니다.
* 두 번째 탭("시리얼없는 매칭")은 메타데이터(모델명 목록, 카운트)만 로드한 채, 사용자가 직접 `[매칭 시작]` 버튼을 누르기 전까지 안전하게 대기 상태를 유지합니다.

---

## [v1.0.0] - 2026-07-16 (초기 배포 버전)
* BNS 자산 대조 및 조정 시스템의 3단 분할 레이아웃(비율 1:1:7) 및 4개 영역 뷰 최초 배포.
* 제조번호(시리얼) 완전 일치 제외 룰 적용.
